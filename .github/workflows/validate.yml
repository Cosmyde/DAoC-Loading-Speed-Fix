# Created by Cosmy.
name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Validate project
        shell: powershell
        run: .\scripts\Test-Project.ps1
